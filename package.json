{
  "name": "pair-bot-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo target repository for the Slack pair-session bot",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
